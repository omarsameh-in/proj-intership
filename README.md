# InternWay Login Page - React + Bootstrap

صفحة تسجيل الدخول لمنصة InternWay مبنية باستخدام React و Next.js و Bootstrap 5.

## 🎨 المميزات

### ✨ التصميم
- تصميم من لوحتين: لوحة يسرى بتدرج لوني جميل (40%) ولوحة تسجيل الدخول (60%)
- شعار متحرك (IW) مع تأثير floating
- تدرجات لونية جميلة (gradients)
- شريط تمرير مخصص (custom scrollbar)

### 🌓 الوضع الداكن والفاتح
- تبديل سهل بين الوضع الداكن والفاتح
- حفظ التفضيلات في localStorage
- تصميم متناسق في كلا الوضعين

### 🌐 دعم متعدد اللغات
- اللغة الإنجليزية والعربية
- دعم كامل لـ RTL (من اليمين لليسار)
- حفظ اللغة المختارة

### 📱 متجاوب تماماً
- تصميم متجاوب لجميع الأجهزة
- إخفاء اللوحة اليسرى على الموبايل
- أحجام مناسبة للشاشات الصغيرة

### 🎯 اختيار الدور
- ثلاثة أدوار: طالب، مرشد، شركة
- أيقونات ملونة مميزة لكل دور
- تغذية بصرية عند الاختيار

### ✅ التحقق من النموذج
- التحقق من البريد الإلكتروني
- حقول مطلوبة
- رسائل خطأ واضحة

## 🚀 التثبيت والتشغيل

### المتطلبات
- Node.js (الإصدار 18 أو أحدث)
- npm أو yarn

### خطوات التثبيت

1. **تثبيت Node.js**
   - حمّل من: https://nodejs.org/
   - اختر النسخة LTS (الموصى بها)

2. **تثبيت المكتبات**
   ```bash
   cd c:\Users\omars\OneDrive\Desktop\in-login\in-login
   npm install
   ```

3. **تشغيل المشروع**
   ```bash
   npm run dev
   ```

4. **فتح المتصفح**
   - افتح: http://localhost:3000/login
   - أو: http://localhost:3000

## 📦 المكتبات المستخدمة

- **React 18** - مكتبة واجهة المستخدم
- **Next.js 14** - إطار عمل React
- **Bootstrap 5.3.2** - إطار عمل CSS
- **React-Bootstrap 2.9.1** - مكونات Bootstrap لـ React
- **Lucide React** - أيقونات جميلة
- **TypeScript** - لغة برمجة مع أنواع

## 🎨 التخصيص

### تغيير الألوان
عدّل الألوان في ملف `app/globals.css`:
- التدرجات اللونية للوحة اليسرى
- ألوان الأزرار
- ألوان شريط التمرير

### إضافة لغات جديدة
عدّل كائن `translations` في `app/login/page.tsx`

## 📁 هيكل الملفات

```
in-login/
├── app/
│   ├── login/
│   │   └── page.tsx          # صفحة تسجيل الدخول الرئيسية
│   ├── globals.css            # الأنماط العامة
│   ├── layout.tsx             # التخطيط الرئيسي
│   └── page.tsx               # الصفحة الرئيسية
├── package.json               # المكتبات والإعدادات
└── README.md                  # هذا الملف
```

## 🔧 الأوامر المتاحة

```bash
npm run dev      # تشغيل المشروع في وضع التطوير
npm run build    # بناء المشروع للإنتاج
npm run start    # تشغيل المشروع المبني
npm run lint     # فحص الأخطاء
```

## 💡 ملاحظات

- الأخطاء في IDE طبيعية قبل تثبيت المكتبات
- بعد `npm install` ستختفي جميع الأخطاء
- المشروع يستخدم Bootstrap بدلاً من Tailwind CSS

## 📝 الترخيص

هذا المشروع خاص بـ InternWay


## 🔌 الربط مع الخلفية (Backend Integration)

هذا المشروع جاهز للربط مع API. تم إعداد `axios` في `app/lib/api.ts` ليقوم تلقائيًا بإرسال الـ Token المخزن.

### المتغيرات البيئية (Environment Variables)
قم بإنشاء ملف `.env` في المجلد الرئيسي وأضف المتغير التالي:
```bash
NEXT_PUBLIC_API_URL=http://localhost:5000
```
(يمكنك نسخ `.env.example`)

### نقاط الاتصال المطلوبة (Endpoints)

يجب أن يوفر الباك إند الروابط التالية:

1. **تسجيل حساب جديد (POST):**
   - `/auth/register/student`
   - `/auth/register/company`
   - `/auth/register/mentor`
   *(يجب أن تدعم هذه الروابط استقبال البيانات ورفع الملفات في حالة الطالب)*

2. **تسجيل الدخول (POST):**
   - `/auth/login`
   - **البيانات المرسلة:** `{ email, password, role }`
   - **الرد المتوقع:** `{ token: "...", user: { ... } }`

---

